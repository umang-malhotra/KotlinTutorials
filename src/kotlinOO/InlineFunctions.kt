package kotlinOO

fun main(){
    /***
     * When an inline function is compiled, it's body is completely substituted to the function call
     *
     * Inlining is often done when a function has lambda expression for a parameter, lambdas requires creation of
     * classes and objects undercover
     *
     * JVM might do inlining even when you haven't defined
     *
     * All we have to do is add inline infront of fun keyword
     */
}